1) client directory has frontEnd
2) if(node_modules) directory isn't available in studentDasboard(i.e main directory)
   then give cmd "npm i"
3) Go to StudentDashboard->client 
   Check for node_modules dir 
   if not there then give cmd "npm i"
4) Above steps will install dependencies 
5) Now come to StudentDashboard dir and give cmd :
      "npm run dev"
   it ll start both frontend react and backend express simultaneously
6) For login -> username : admin
                password : admin